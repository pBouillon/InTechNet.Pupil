/**
 * @summary Contains all keys used for local storage
 * @see LocalStorageService
 */
export enum LocalStorageKeys {

    /**
     * @summary key to access pupil's data in local storage
     */
   CURRENT_PUPIL = 'currentPupil',

}
